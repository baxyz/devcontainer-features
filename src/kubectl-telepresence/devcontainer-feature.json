{
  "version": "0.0.1",
  "id": "kubectl-telepresence",
  "name": "kubectl & Telepresence",
  "description": "Installs kubectl and Telepresence without unnecessary dependencies like Helm or Minikube. Optimized for development with Kubernetes clusters.",
  "documentationURL": "https://github.com/baxyz/devcontainer-features/tree/main/src/kubectl-telepresence",
  "options": {
    "kubectl": {
      "type": "string",
      "default": "latest",
      "description": "Select or enter a kubectl version. (Available versions may vary by repository)"
    },
    "telepresence": {
      "type": "string",
      "default": "latest",
      "description": "Select or enter a Telepresence version"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-kubernetes-tools.vscode-kubernetes-tools",
        "ms-azuretools.vscode-docker"
      ]
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}